# Splits

Reproducible split manifests will be stored here when the pipeline is
implemented. VSB splits must group by `id_measurement`. Rauscher splits must
preserve the original partitions and reserve `Te2`.
