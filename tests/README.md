# Tests

- `unit/`: isolated functions.
- `integration/`: interaction between components.
- `data_contracts/`: schema, shapes, labels, partitions, and manifests.
- `regression/`: previously validated results or behavior.
