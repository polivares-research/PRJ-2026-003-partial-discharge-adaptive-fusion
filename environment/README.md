# Environment

Definitions are centralized in ResearchHub Atlas. Use `researchctl env create`, then attach one with `researchctl env attach PRJ-YYYY-NNN ENV-<NAME>-NNN`. Physical instances live outside this repository under the configured environment root.
