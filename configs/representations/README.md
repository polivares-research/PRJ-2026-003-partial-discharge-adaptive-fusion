# Representaciones previstas

The initial comparison matrix includes:

- raw waveform;
- CWT;
- STFT/spectrogram;
- SWT/DWT;
- PSD and handcrafted statistical/spectral features.

The transformations will later be implemented in `src/`, with their parameters
defined here rather than inside notebooks.
