# Use an environment variable when data are stored outside the repository.
# The default runtime location is data/raw.
raw_data_root: ${PD_RAW_DATA_ROOT:-data/raw}

datasets:
  vsb: ${PD_RAW_DATA_ROOT:-data/raw}/dataset-vsb-power-line-fault-detection
  pd_noise: ${PD_RAW_DATA_ROOT:-data/raw}/dataset-pd-noise
